import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
    container: {
        textAlign: "center",
        margin: "30px auto",
        maxWidth: "900px"
    },
    iconImg: {
        width: "180px",
        height: "150px"
    }
}));